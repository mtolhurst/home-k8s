# apiVersion: argoproj.io/v1alpha1
# kind: Application
# metadata:
#   name: descheduler
#   namespace: argocd
#   finalizers:
#     - resources-finalizer.argocd.argoproj.io 
# spec:
#   syncPolicy:
#     syncOptions:
#       - CreateNamespace=true
#     automated: 
#       prune: true 
#       selfHeal: true
#       allowEmpty: false 
#   destination:
#     namespace: descheduler
#     server: {{ .Values.spec.destination.server }}
#   project: default
#   sources:
#     - chart: descheduler
#       repoURL: https://kubernetes-sigs.github.io/descheduler/
#       targetRevision: 0.36.0
#       helm:
#         valuesObject:
#           kind: CronJob
#           schedule: "*/30 * * * *" 
#           deschedulerPolicy:
#             apiVersion: "descheduler/v1alpha2"
#             kind: "DeschedulerPolicy"
#             profiles:
#               - name: DefaultProfile
#                 plugins:
#                   balance:
#                     enabled:
#                       - "LowNodeUtilization"
#                 pluginConfig:
#                   - name: "LowNodeUtilization"
#                     args:
#                       thresholds:
#                         "cpu": 30
#                         "memory": 30
#                       targetThresholds:
#                         "cpu": 60
#                         "memory": 60
# TODO commented out for now. This is useless, just using requests I believe.
# Only add this once we get around to wiring it up with prometheus
